# Ethereum Node Setup Guide (Geth)

This document explains how to deploy and run an Ethereum full node using Geth.

## 1. Install Geth

```bash
sudo add-apt-repository -y ppa:ethereum/ethereum
sudo apt-get update
sudo apt-get install ethereum
